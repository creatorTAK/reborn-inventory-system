# Cloudflare Workers Configuration
# REBORN FCM Worker - Push Notifications & Email (v2 - FCM HTTP v1 API)

name = "reborn-fcm-worker"
main = "fcm-worker.js"
compatibility_date = "2025-11-08"
account_id = "11bd3a44e361a1ed3c3c9a860c93f612"

# Environment variables (Secrets)
# Set these using: wrangler secret put <NAME>
#
# Required for push notifications (FCM HTTP v1 API):
# - FIREBASE_SERVICE_ACCOUNT (Firebase Service Account JSON string)
# - FIREBASE_PROJECT_ID (e.g., "reborn-chat")
#
# Required for email:
# - RESEND_API_KEY (Resend API key for email)
#
# Note: FCM_SERVER_KEY is no longer used (legacy API deprecated June 2024)
